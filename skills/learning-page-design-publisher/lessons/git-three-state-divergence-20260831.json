{
  "LESSON_ID": "git-three-state-divergence-20260831",
  "META_DESCRIPTION": "从 Git 对象数据库、refs/HEAD、index/working tree、commit DAG、merge-base 与 ahead/behind 出发，系统理解本地、GitHub 与服务器状态为什么会分叉，以及如何安全收敛到可部署主线。",
  "TITLE": "Git 三个状态为什么会不一致？",
  "SUBTITLE": "不把 Git 当成“pull / reset 命令集合”：用对象图、引用、暂存区、工作区和部署产物五个层次，解释本地前端、GitHub main 与服务器 checkout 为什么可以同时正确却不相等。",
  "DOMAIN": "Git / Version Control / DevOps",
  "MODE": "/learn · system",
  "DURATION": "50–70 分钟",
  "DIFFICULTY": "中等 → 深入",
  "DATE": "2026-08-31",
  "TERMS_HTML": "<style data-lesson-extra>math[display=\"block\"]{display:block;max-width:100%;overflow-x:auto;overflow-y:hidden;padding:.35rem 0}.sec{overflow-wrap:anywhere}@media(max-width:820px){math[display=\"block\"]{font-size:.95em}}</style><details><summary>Object Database：blob / tree / commit</summary><div class=\"body\"><dl><dt>直觉</dt><dd>Git 的核心不是“保存很多文件夹副本”，而是保存不可变对象，并用哈希把对象连接起来。</dd><dt>严格定义</dt><dd>blob 保存文件内容；tree 保存目录结构和文件名到对象的映射；commit 指向一个根 tree、零个或多个父 commit，并携带作者、提交者和消息等元数据。对象通过内容寻址，被 SHA-1/SHA-256 形式的对象 ID 标识（具体仓库格式取决于 Git 配置）。</dd><dt>比喻</dt><dd>像数学里的不可变节点图：commit 是节点，父指针是有向边；分支名只是给某个节点贴的可移动标签。</dd><dt>边界</dt><dd>分支名不是提交内容本身；删除一个 branch ref 也不一定立即删除 commit 对象。反过来，工作区里看到的文件也不是 Git 历史本身。</dd><dt>本案作用</dt><dd>理解对象不可变之后，就容易看懂：<code>main</code>、<code>origin/main</code>、<code>HEAD</code> 只是指向对象图某处的可移动引用。</dd></dl></div></details><details><summary>Reference（ref）与 Branch</summary><div class=\"body\"><dl><dt>直觉</dt><dd>branch 本质上是一个会移动的名字，它通常指向某个 commit。</dd><dt>严格定义</dt><dd>本地分支一般存于 <code>refs/heads/*</code>；tag、remote-tracking ref 等属于其他引用命名空间。创建新 commit 时，当前分支 ref 会移动到新 commit。</dd><dt>比喻</dt><dd>像书签：书签名字可以叫 main，但不同人的书里同名书签完全可能夹在不同页。</dd><dt>边界</dt><dd>“分支包含哪些文件”是口语简化。严格地说，分支指向一个 commit，commit 再指向快照。两个分支可以指向同一 commit，也可以后来发生分叉。</dd><dt>本案作用</dt><dd>比较本地 <code>main</code>、GitHub <code>main</code> 和服务器分支时，不要把名字相同当作对象相同，最终要比较它们各自解析到的 commit ID。</dd></dl></div></details><details><summary>HEAD 与 Detached HEAD</summary><div class=\"body\"><dl><dt>直觉</dt><dd>HEAD 表示“当前这个工作目录站在哪里”。通常它指向一个本地分支，也可以直接指向某个 commit。</dd><dt>严格定义</dt><dd>HEAD 常常是 symbolic ref，例如指向 <code>refs/heads/main</code>。detached HEAD 时，HEAD 直接指向 commit；这时新提交仍会产生对象，但没有普通分支名自动跟随它们。</dd><dt>比喻</dt><dd>像你当前打开的书页：别人把目录索引更新了，不会自动把你手里的书翻到那一页。</dd><dt>边界</dt><dd>HEAD 不等于 <code>origin/main</code>，也不等于服务器正在运行的容器镜像。Git 工作目录当前 commit 与实际生产进程版本仍需单独核验。</dd><dt>本案作用</dt><dd>服务器上 <code>origin/main</code> 已 fetch 到新 SHA，但 HEAD 仍在旧分支时，工作树不会因为 fetch 自动切到新代码。</dd></dl></div></details><details><summary>Index / Staging Area</summary><div class=\"body\"><dl><dt>直觉</dt><dd>Git 在工作区和 commit 之间还有一层“下一次准备提交什么”的索引。</dd><dt>严格定义</dt><dd>index 保存下一次 commit 快照的候选状态。<code>git add</code> 把工作区内容写入对象库并更新 index；<code>git commit</code> 基于 index 创建新的 tree/commit。</dd><dt>比喻</dt><dd>像交作业：工作区是草稿纸，index 是你放进提交袋里的版本，commit 是正式封存。</dd><dt>边界</dt><dd>工作区 clean 需要同时考虑“index 相对 HEAD 无 staged 变化”和“working tree 相对 index 无 unstaged 变化”。只看有没有未跟踪文件或只看 diff 一种方向都可能漏信息。</dd><dt>本案作用</dt><dd>当你说服务器“工作树干净”，严格含义只是本地未提交状态干净，不能推出分支最新、远端同步、生产部署正确。</dd></dl></div></details><details><summary>Working Tree 与 clean / dirty</summary><div class=\"body\"><dl><dt>直觉</dt><dd>working tree 是你在磁盘上实际编辑的文件；它可以和 HEAD、index 不同。</dd><dt>严格定义</dt><dd><code>git status</code> 通过比较 HEAD↔index、index↔working tree，再结合 untracked 文件来呈现未提交状态。clean 通常意味着没有待提交、待暂存的已跟踪变化以及需要提示的未跟踪变化。</dd><dt>比喻</dt><dd>像桌面收拾得很整齐，不代表你桌上的教材是最新版。</dd><dt>边界</dt><dd>clean 只描述“相对当前 HEAD 的本地修改状态”，完全不回答 HEAD 是否落后远端、是否在错误分支、是否对应部署中的进程。</dd><dt>本案作用</dt><dd>106 服务器 clean 但 HEAD 在旧 commit 时，结论应该是“旧版本被干净地检出”，不是“服务器已经同步最新版”。</dd></dl></div></details><details><summary>Remote-tracking ref：origin/main</summary><div class=\"body\"><dl><dt>直觉</dt><dd>它是本地对“上次 fetch 时远端 main 在哪里”的记录，不是远端本身，也不是当前 checkout。</dd><dt>严格定义</dt><dd><code>refs/remotes/origin/main</code> 通常由 <code>git fetch origin</code> 更新。它用于比较和作为新分支基线；普通开发不直接在这个 ref 上提交。</dd><dt>比喻</dt><dd>像本地缓存的一份列车时刻表：fetch 是刷新时刻表，checkout/switch 才是你真正换乘。</dd><dt>边界</dt><dd>如果很久没 fetch，<code>origin/main</code> 可能陈旧；如果刚 fetch，它可以准确反映远端当时状态，但 HEAD 仍然不会自动移动。</dd><dt>本案作用</dt><dd>服务器 <code>origin/main = GitHub main</code> 只能证明“服务器知道远端最新位置”，不能证明服务器实际文件或运行进程已更新。</dd></dl></div></details><details><summary>Commit DAG、Ancestor 与 Reachability</summary><div class=\"body\"><dl><dt>直觉</dt><dd>Git 历史是有向无环图，不是一条“最新到最旧”的单线时间轴。</dd><dt>严格定义</dt><dd>commit 指向父 commit，因此从某个 commit 沿父边可到达的集合就是它的祖先历史。若 A 可沿父边到达 B，则 B 是 A 的 ancestor。merge commit 可有两个或更多父。</dd><dt>比喻</dt><dd>像偏序集：祖先关系给出偏序，不是所有 commit 都能比较出“谁包含谁”。</dd><dt>边界</dt><dd>提交时间不决定祖先关系。一个较晚创建的 commit 可以基于较旧分支，与另一条历史互不为祖先。</dd><dt>本案作用</dt><dd>判断能否 fast-forward、哪些提交只存在一侧、是否真正分叉，都应该基于 DAG 可达性而不是时间戳或 SHA 大小。</dd></dl></div></details><details><summary>Merge Base 与 ahead / behind</summary><div class=\"body\"><dl><dt>直觉</dt><dd>比较两条分支时，先找到共同祖先，再数双方各自独有的可达提交。</dd><dt>严格定义</dt><dd><code>git merge-base A B</code> 找最佳共同祖先；<code>A...B</code> 表示对称差历史，常配合 <code>git rev-list --left-right --count A...B</code> 统计双方独有提交数。</dd><dt>比喻</dt><dd>像集合论：从共同祖先开始，计算两边的集合差，而不是比较两个集合最后一个元素的时间。</dd><dt>边界</dt><dd>ahead/behind 是相对于某一对 refs 的计数，不是仓库的全局属性，也不表示代码质量、时间新旧或文件变化量。不同比较对出现 631/1 和 621/66 完全不矛盾。</dd><dt>本案作用</dt><dd>案例里应该分别写清“GitHub main vs 106 HEAD”和“GitHub main vs 本地前端分支”，不能把两个数字拼成一个故事。</dd></dl></div></details><details><summary>Fast-forward、Merge、Rebase、Cherry-pick</summary><div class=\"body\"><dl><dt>直觉</dt><dd>这些操作都能“把改动带到另一条历史”，但它们改变图的方式不同。</dd><dt>严格定义</dt><dd>fast-forward 只是移动 ref，不创建 merge commit；merge 把两条历史通过新 merge commit 汇合；rebase 重新复制一串 commit 到新基线上，产生新的 commit IDs；cherry-pick 把选定提交的补丁语义应用到当前基线并创建新 commit。</dd><dt>比喻</dt><dd>像证明重组：merge 保留两条推导路径并在末端合流；rebase 是把一串推导重新建立在新公理基线上。</dd><dt>边界</dt><dd>已经共享的公共历史随意 rebase 会改变 commit identity，可能给协作者造成重复和冲突；merge 也不是永远优于 rebase。选择依据是历史协作语义，而不是“哪个命令更高级”。</dd><dt>本案作用</dt><dd>把前端 66 个独有提交合入最新 main，可以选择 merge 或有计划地重放；服务器 1 个独有热修若仍有价值，可能更适合单独 cherry-pick。</dd></dl></div></details><details><summary>reset / restore / revert</summary><div class=\"body\"><dl><dt>直觉</dt><dd>这三个命令名字都像“撤销”，但作用对象和历史语义完全不同。</dd><dt>严格定义</dt><dd><code>git reset</code> 主要移动当前分支/HEAD，并可同时改变 index 和 working tree；<code>git restore</code> 针对工作区/index 内容恢复；<code>git revert</code> 新建一个反向提交来抵消已有 commit 的效果。</dd><dt>比喻</dt><dd>像改账本：reset 是把“当前指针”挪回旧页并可撕掉草稿；revert 是在新页记一笔相反分录。</dd><dt>边界</dt><dd><code>reset --hard</code> 会丢弃当前 index/working tree 的修改，并可能把分支移走；对尚未保护的独有历史非常危险。revert 不重写已有共享历史，但可能产生冲突。</dd><dt>本案作用</dt><dd>在 106 独有提交尚未建 backup ref 前，不应把 hard reset 当“同步服务器”的第一步。</dd></dl></div></details><details><summary>worktree、stash 与可恢复引用</summary><div class=\"body\"><dl><dt>直觉</dt><dd>一个仓库可以同时有多个工作目录；stash 也能临时保存工作区/index 状态，但它们都不是“自动合并来源”。</dd><dt>严格定义</dt><dd><code>git worktree</code> 允许同一仓库对象库挂载多个工作目录，每个 worktree 有自己的 HEAD/index；stash 记录工作区/index 的临时快照。两者都需要显式检查，不能因为主 worktree clean 就假设别处无工作。</dd><dt>比喻</dt><dd>像同一本资料库开了多个实验台：主实验台整理好，不代表隔壁实验台没有唯一样品。</dd><dt>边界</dt><dd>删除 worktree、清理 stash 或回收不可达对象前，应确认是否存在唯一数据。reflog 提供一定恢复机会，但恢复能力不是数据治理方案。</dd><dt>本案作用</dt><dd>当仓库存在服务器临时 worktree、历史 stash 或 Agent 生成分支时，收敛前先做 inventory，确定哪些是 refs、哪些是未提交状态、哪些只是缓存。</dd></dl></div></details><details><summary>部署版本 ≠ Git checkout 版本</summary><div class=\"body\"><dl><dt>直觉</dt><dd>服务器目录里的 HEAD 只是源码状态，真正对外服务的进程、二进制或镜像可能来自另一个 commit。</dd><dt>严格定义</dt><dd>部署通常还包含 build artifact、容器镜像、systemd 进程、环境变量和数据库 schema。可靠发布应能把运行版本映射回 source commit，例如注入 Git SHA、镜像 digest 或 build metadata。</dd><dt>比喻</dt><dd>像源码是菜谱，正在端上桌的菜是构建产物；你换了菜谱，不代表厨房里那锅菜自动重做。</dd><dt>边界</dt><dd>即使 <code>HEAD = origin/main</code>，如果旧 Docker 容器没有重建/重启，对外服务仍可能运行旧代码；反过来，工作目录被改动也不一定改变正在运行的二进制。</dd><dt>本案作用</dt><dd>案例最终验收不能只看三个 SHA 相等，还要验证生产进程/镜像可追溯到同一个发布 commit 并通过 health/smoke。</dd></dl></div></details>",
  "ORIENTATION_HTML": "\n<p><strong>一句话：</strong>Git 的“状态不一致”不是一个 bug，而是它允许多个引用、多个工作区和多个历史分支独立演化的直接结果。真正的问题是：你有没有把每个状态变量说清楚。</p>\n<div class=\"call\"><strong>把 Git 仓库抽象成四元组：</strong><code>S = (G, R, I, W)</code>。其中 <code>G</code> 是不可变对象组成的提交图，<code>R</code> 是 branches/tags/remote-tracking refs 等可移动引用，<code>I</code> 是 index，<code>W</code> 是 working tree。大多数 Git 困惑都来自把这四个东西混成“代码版本”。</div>\n<p>例如，“服务器已经 fetch 最新 main”只改变了 <code>R</code> 里的 <code>origin/main</code>；它不会自动移动当前 HEAD，不会自动改 index/working tree，更不会自动重建 Docker 镜像或重启生产进程。于是完全可能同时出现：</p>\n<pre><code>GitHub main       = 84382576\nserver origin/main= 84382576   # 服务器已经知道远端在哪\nserver HEAD       = c5e35ae    # 服务器实际 checkout 仍在别处\nworking tree      = clean      # 但相对 c5e35ae 没有未提交改动\nruntime image     = ???        # 还需要单独证明</code></pre>\n<div class=\"grid\">\n  <div class=\"mini fact\"><h3>第一层：对象与历史</h3><p>commit 不会因为分支移动而“变成另一个 commit”。先保护独有对象，再考虑把引用收敛。</p></div>\n  <div class=\"mini infer\"><h3>第二层：引用关系</h3><p>main、origin/main、HEAD 都只是名字/指针。比较它们必须解析到具体 commit，并检查祖先/可达关系。</p></div>\n  <div class=\"mini risk\"><h3>第三层：未提交状态</h3><p>index、working tree、untracked、stash、worktree 可能保存唯一内容；hard reset 之前必须先盘点。</p></div>\n</div>\n<h3>clean 的严格含义为什么很窄</h3>\n<p>简化地说，如果把当前 HEAD 快照记作 <code>T_H</code>、index 快照记作 <code>T_I</code>、工作区可跟踪内容记作 <code>T_W</code>，那么“没有 staged/unstaged 修改”要求它们在相关比较上相等：</p>\n<p><math display=\"block\"><msub><mi>T</mi><mi>H</mi></msub><mo>=</mo><msub><mi>T</mi><mi>I</mi></msub><mo>=</mo><msub><mi>T</mi><mi>W</mi></msub></math></p>\n<p>但这个等式里根本没有 <code>origin/main</code>。所以 <strong>clean 与 up-to-date 是两个正交命题</strong>：一个回答“有没有本地未提交变化”，另一个回答“当前 commit 与某个远端 ref 的图关系是什么”。</p>\n<div class=\"call warn\"><strong>工程目标：</strong>不要追求“所有 SHA 越快一样越好”。收敛顺序应该是：先知道独有数据在哪里 → 建立可恢复引用 → 在非生产分支集成 → 验证 → 更新权威 main → 最后部署并证明运行版本。</div>\n",
  "CONCEPT_MAP_HTML": "\n<div class=\"flow\">\n<span class=\"node\">blob / tree / commit</span>→\n<span class=\"node\">Commit DAG</span>→\n<span class=\"node\">refs / branch</span>→\n<span class=\"node\">HEAD</span>→\n<span class=\"node\">index</span>→\n<span class=\"node\">working tree</span>\n</div>\n<div class=\"flow\" style=\"margin-top:10px\">\n<span class=\"node\">remote GitHub main</span>→\n<span class=\"node\">git fetch</span>→\n<span class=\"node\">origin/main</span>→\n<span class=\"node\">merge / rebase / ff</span>→\n<span class=\"node\">local main / HEAD</span>→\n<span class=\"node\">build / deploy</span>→\n<span class=\"node\">runtime artifact</span>\n</div>\n<div class=\"call\" style=\"margin-top:14px\"><strong>两个图同时存在：</strong>上面第一行是 Git 内部数据模型；第二行是协作/交付数据流。理解第一行能解释命令语义，理解第二行能避免把“fetch 了”误解成“服务器已经发布”。</div>\n",
  "MINIMUM_KNOWLEDGE_HTML": "\n<h3>最小知识闭包 1：分支关系是偏序，不是时间排序</h3>\n<p>设提交图为 DAG <code>G=(V,E)</code>，边从子 commit 指向父 commit。定义 <code>B ≼ A</code> 表示从 A 沿父边可以到达 B，也就是 B 是 A 的 ancestor。这个关系是偏序：有些 A、B 互不为祖先，它们发生了真正分叉。</p>\n<p>因此“哪个 commit 时间更晚”不能替代“哪个包含哪个”。fast-forward 是否可能，只取决于祖先关系：如果当前 commit 是目标 commit 的 ancestor，就可以只移动 ref；否则需要 merge、rebase、cherry-pick 或其他显式集成。</p>\n\n<h3>最小知识闭包 2：ahead / behind 的数学含义</h3>\n<p>给定两个 tips A、B 和共同基线 M，粗略可以理解：</p>\n<p><math display=\"block\"><mi>ahead</mi><mo>(</mo><mi>A</mi><mo>,</mo><mi>B</mi><mo>)</mo><mo>=</mo><mo>|</mo><mi>Reach</mi><mo>(</mo><mi>A</mi><mo>)</mo><mo>∖</mo><mi>Reach</mi><mo>(</mo><mi>B</mi><mo>)</mo><mo>|</mo></math></p>\n<p><math display=\"block\"><mi>behind</mi><mo>(</mo><mi>A</mi><mo>,</mo><mi>B</mi><mo>)</mo><mo>=</mo><mo>|</mo><mi>Reach</mi><mo>(</mo><mi>B</mi><mo>)</mo><mo>∖</mo><mi>Reach</mi><mo>(</mo><mi>A</mi><mo>)</mo><mo>|</mo></math></p>\n<pre><code>git merge-base A B\ngit rev-list --left-right --count A...B\ngit log --oneline --left-right A...B</code></pre>\n<p>所以“server 比 GitHub behind 631、ahead 1”和“frontend 比 GitHub behind 621、ahead 66”描述的是两组不同集合差，完全可以同时成立。</p>\n\n<h3>最小知识闭包 3：fetch、pull、switch、merge 分别改变什么</h3>\n<div class=\"table\"><table><tr><th>操作</th><th>主要改变 refs</th><th>index / working tree</th><th>历史图</th><th>关键风险</th></tr><tr><td><code>git fetch</code></td><td>更新 remote-tracking refs，如 origin/main</td><td>通常不改当前工作区</td><td>下载对象但不重写本地分支</td><td>低；但 fetch 后仍要理解分叉</td></tr><tr><td><code>git switch branch</code></td><td>改变 HEAD 指向</td><td>检出目标分支快照</td><td>不创建新历史</td><td>未提交变化可能阻止或被带过去</td></tr><tr><td><code>git merge B</code></td><td>成功后移动当前分支</td><td>更新到合并结果</td><td>可能新建 merge commit</td><td>冲突需要逐项解决</td></tr><tr><td><code>git rebase B</code></td><td>当前分支最终指向新复制 commits</td><td>随过程更新</td><td>重写 commit IDs</td><td>共享历史需要谨慎</td></tr><tr><td><code>git reset --hard X</code></td><td>移动当前分支/HEAD</td><td>强制改 index 与 working tree</td><td>旧 commit 可能变为仅 reflog 可达</td><td>高：未保护数据可丢</td></tr><tr><td><code>git revert X</code></td><td>新增 commit 后移动当前分支</td><td>更新为反向变更结果</td><td>不删除原历史</td><td>语义冲突但共享历史更安全</td></tr></table></div>\n<h3>最小知识闭包 4：为什么 reflog 不是备份策略</h3>\n<p>reflog 记录本地 ref/HEAD 曾经指向哪里，很多误操作后确实能用于恢复。但 reflog 有保留期限、是本地性的，也可能在垃圾回收后失去对象。成熟流程应该在危险操作前主动建立明确 branch/tag 或远端备份 ref，而不是事后祈祷 reflog 还在。</p>\n<pre><code>git branch backup/server-20260831 c5e35ae\ngit branch backup/frontend-20260831 a0580dec\ngit show --stat backup/server-20260831\ngit log --oneline origin/main..backup/server-20260831</code></pre>\n\n<h3>最小知识闭包 5：冲突数量不是“谁对谁错”</h3>\n<p>48 个冲突文件只说明 Git 无法自动合并那些路径/片段。它既不证明前端分支错误，也不证明 main 应该被覆盖。冲突解决是一个语义判断：在最新 main 的架构约束上，哪些旧改动仍代表你真正想保留的行为？</p>\n<div class=\"call danger\"><strong>批量 ours/theirs 的危险：</strong>它把“解决冲突”退化成“选一边覆盖”，会丢失另一边非冲突但仍相关的语义。正确做法是先理解双方变化，再逐功能验证。</div>\n\n<h3>最小知识闭包 6：Git 收敛和生产收敛是两件事</h3>\n<p>设最终权威 commit 为 M。Git 收敛可以要求 <code>local main = origin/main = GitHub main = M</code>；但生产收敛还要增加 <code>runtime_source = M</code> 或至少可验证的 artifact digest → M 映射。否则你会得到“仓库看起来全对，线上仍旧”的假阳性。</p>\n\n<h3>最小知识闭包 7：三方合并为什么需要 merge base</h3>\n<p>Git 合并不是简单比较 A 和 B 两份最终文件，然后决定谁覆盖谁。典型三方合并会同时看共同祖先 M、当前分支 A、待合并分支 B：分别计算 M→A 与 M→B 的变化，再尝试组合。只有两边对同一语义区域做了无法自动协调的修改时，才产生冲突。</p>\n<p>这解释了一个重要现象：两个分支最终文件看起来差异巨大，也不一定产生很多冲突；反过来，即使只改了少量行，只要双方碰到同一上下文，也可能冲突。冲突数量因此不能直接当“分支质量分数”。</p>\n\n<h3>最小知识闭包 8：commit identity 和 patch identity 不是一回事</h3>\n<p>rebase、cherry-pick 会创建新的 commit，即使代码变化“看起来一样”，commit ID 也通常不同，因为父提交、元数据等发生变化。因此判断“这个修复是否已经以等价补丁进入 main”有时不能只用 SHA 相等，还需要看 patch、文件 diff、测试和功能证据。</p>\n<p>这也是为什么大规模历史整理后，旧分支可能显示很多“独有 commits”，但其中部分语义已经以新 commit 形式进入主线。此时不能机械地把所有旧 commit 再 cherry-pick 一遍，否则可能重复应用同一修改。</p>\n\n<h3>最小知识闭包 9：删除 branch 为什么不等于删除历史</h3>\n<p>branch 只是 ref。删除 branch 主要删除一个名字；只要其 commit 仍被其他 refs、reflog 或对象可达关系引用，历史对象仍可能存在。但如果某一串 commit 只有这个 ref 能稳定到达，删除后就降低了可发现性和可恢复性，未来垃圾回收也可能清理不可达对象。</p>\n<p>所以“这个分支看起来没用了”不是删除依据。先用 <code>git branch --merged</code>、<code>git log main..branch</code>、比较 patch/功能证据确认没有独有价值，再做清理。</p>\n",
  "CASE_RECONSTRUCTION_HTML": "\n<h3>案例数据</h3>\n<pre><code>共同基线：36b470e7\n\nGitHub main：84382576\n  └─ 从共同基线独有 +621 commits\n\n本地 frontend：a0580dec\n  └─ 从共同基线独有 +66 commits\n  └─ 与最新 main 试合并：48 个冲突文件\n\n106 server HEAD：c5e35ae\n  └─ 相对某比较基线有独有 +1 commit\n\n106 origin/main：84382576\n  └─ 已 fetch 到 GitHub main\n\n另有临时 worktree：0fb8cad7\n  └─ 相对 main 落后若干 commit</code></pre>\n\n<h3>第一问：服务器到底“同步了没有”？</h3>\n<p>答案必须拆成三句话：服务器的 <code>origin/main</code> 已同步到远端观察结果；服务器的当前 HEAD 没有同步到 main；实际运行服务版本未知，需要继续查构建/容器/进程。只说“同步了”或“没同步”都过于粗糙。</p>\n\n<h3>第二问：为什么不能先在 106 上 reset --hard？</h3>\n<p>因为服务器有一个独有 commit <code>c5e35ae</code>。在确认这个 commit 的内容、价值和备份引用之前，hard reset 会把当前分支直接移到另一个点并覆盖工作区/index。即使对象暂时可通过 reflog 找回，也是在主动降低可恢复性。</p>\n\n<h3>第三问：前端 66 个 commit 应该怎么进 main？</h3>\n<p>不是把“66 个 commit 全保留”作为目标，而是把“这些历史中仍然需要的产品/工程行为”作为目标。先从最新 main 创建 integration 分支，在非生产环境做合并或重放，处理冲突后跑测试。若历史非常嘈杂，也可以通过分段 cherry-pick、重新整理 commit 或将功能重新实现到新架构，但必须保留审计线索。</p>\n\n<h3>第四问：临时 worktree 为什么也要清点？</h3>\n<p>因为每个 worktree 有自己的 HEAD/index/working tree。主目录 clean 不代表临时 worktree 没有独有未提交改动。收敛前应执行：</p>\n<pre><code>git worktree list --porcelain\ngit status --short\ngit stash list\ngit branch --all --verbose --no-abbrev</code></pre>\n<p>再分别进入需要检查的 worktree 查看状态。</p>\n\n<h3>第五问：怎么把事实记录成机器可验证证据？</h3>\n<pre><code># refs\ngit rev-parse HEAD\ngit rev-parse refs/remotes/origin/main\n\n# 图关系\ngit merge-base HEAD origin/main\ngit rev-list --left-right --count HEAD...origin/main\n\n# 独有历史\ngit log --oneline origin/main..HEAD\ngit log --oneline HEAD..origin/main\n\n# 未提交状态\ngit status --porcelain=v1\ngit diff\ngit diff --cached</code></pre>\n<p>这些命令分别回答“指针在哪里”“图关系是什么”“谁有独有 commits”“工作区/index 是否有独有未提交状态”，比一句“看起来差不多”更适合 Agent/CI 自动验收。</p>\n\n<div class=\"call success\"><strong>案例的真正目标：</strong>把一个看似杂乱的 Git 状态问题，转化成对象图、refs、未提交状态、构建产物四张清单；然后只对有独有信息的地方做保护和集成。</div>\n",
  "CONCEPT_CASE_MAPPING_HTML": "<div class=\"table\"><table><tr><th>你听到的口语</th><th>严格翻译</th><th>应该用什么证据</th></tr><tr><td>“服务器已经拉到最新了”</td><td>是 origin/main 更新了，还是 HEAD/main 更新了，还是运行进程更新了？</td><td><code>rev-parse</code> + runtime version</td></tr><tr><td>“工作树是干净的”</td><td>HEAD↔index、index↔working tree 没有未提交差异</td><td><code>git status --porcelain</code></td></tr><tr><td>“这个分支更新”</td><td>它和比较对象的 ancestor/reachability 关系是什么？</td><td><code>merge-base</code>、<code>rev-list ...</code></td></tr><tr><td>“本地领先 66 个”</td><td>相对指定 ref 有 66 个仅本地可达 commits</td><td><code>rev-list --left-right --count</code></td></tr><tr><td>“冲突 48 个文件”</td><td>自动三方合并有 48 个路径需要人工语义决策</td><td>dry-run/integration merge + conflict list</td></tr><tr><td>“把服务器同步到 main”</td><td>保护独有状态后，使部署来源收敛到已验收的 main commit</td><td>backup refs + fast-forward/deploy + smoke</td></tr><tr><td>“Git 已经一样了”</td><td>refs 相等不代表运行产物一致</td><td>Git SHA + image digest/build metadata + health</td></tr></table></div>\n<h3>危险命令按“是否会销毁当前可见状态”分类</h3>\n<div class=\"table\"><table><tr><th>类别</th><th>典型命令</th><th>默认策略</th></tr><tr><td>只读观察</td><td><code>status</code> / <code>log</code> / <code>diff</code> / <code>rev-parse</code> / <code>merge-base</code></td><td>优先使用，先建立事实</td></tr><tr><td>增加可恢复性</td><td><code>branch backup/...</code> / tag / push backup ref</td><td>在重写或丢弃前执行</td></tr><tr><td>可控集成</td><td>merge / cherry-pick / rebase on integration branch</td><td>在非生产工作区进行并测试</td></tr><tr><td>破坏性收敛</td><td><code>reset --hard</code> / clean / branch -D</td><td>只有独有信息已证明被保护且目标明确时才用</td></tr></table></div>\n<h3>给 AI Coding Agent 的验收合同</h3>\n<pre><code>1. 不允许根据“branch 名相同”宣称版本一致。\n2. 必须报告 HEAD / local main / origin/main 的完整 SHA。\n3. 必须报告 ahead/behind 的比较对。\n4. 发现独有 commit、stash、dirty worktree 时禁止先 hard reset。\n5. 集成在非生产分支完成并通过测试后，才允许更新 canonical main。\n6. 部署完成必须读回 runtime version / image digest 和 health。</code></pre>\n",
  "EXERCISE_HTML": "<div class=\"q\"><span class=\"num\">1</span>服务器执行 <code>git fetch</code> 后显示 <code>origin/main</code> 已是最新 SHA，但 <code>git rev-parse HEAD</code> 仍是旧 SHA。请精确描述 fetch 改变了什么、没有改变什么。</div><div class=\"q\"><span class=\"num\">2</span>为什么 <code>git status</code> 显示 clean 不能推出“服务器已经是 GitHub 最新代码”？请用 <code>HEAD / index / working tree / origin/main</code> 四个对象回答。</div><div class=\"q\"><span class=\"num\">3</span>A 分支相对 B 显示 ahead 66 / behind 621。请用可达集合解释这两个数字，不要用“新旧”这种模糊词。</div><div class=\"q\"><span class=\"num\">4</span>什么时候 fast-forward 是可能的？什么时候必须进行真正的历史集成？请用 ancestor 关系表达。</div><div class=\"q\"><span class=\"num\">5</span>服务器有一个未推送独有 commit。为什么先创建 backup branch 比“反正还有 reflog”更合理？</div><div class=\"q\"><span class=\"num\">6</span>merge、rebase、cherry-pick 都能把改动带到新基线。对于公开共享历史、大批前端历史、单个服务器热修，分别更可能怎么选？说明理由。</div><div class=\"q\"><span class=\"num\">7</span>如果最终 <code>HEAD = origin/main = GitHub main</code>，但用户仍看到旧版本，Git 层之外至少还需要检查哪三个对象？</div><div class=\"q\"><span class=\"num\">8</span>请为一个由 Coding Agent 自动执行的“服务器与 main 收敛”流程写 6 条不可违反的安全不变量。</div>",
  "ORIENT_CHECKLIST_LABEL": "我已经能把 Git 状态写成对象图、refs、index、working tree，而不是笼统说“版本不一致”。",
  "TERMS_CHECKLIST_LABEL": "我已经能区分 commit 对象、branch/ref、HEAD、origin/main、index、working tree、merge-base 与运行产物。",
  "CASE_CHECKLIST_LABEL": "我已经能在不破坏独有历史的前提下，设计从盘点、保护、集成、验收到部署的收敛流程。",
  "HINT_1_HTML": "<p>先禁止自己想 <code>pull/reset</code>。把状态拆成：对象图、refs、index/working tree、worktree/stash、runtime artifact，然后分别问“哪里有独有信息”。</p>",
  "HINT_2_HTML": "<p>用 ancestor/reachability 解释 ahead/behind；用 backup refs 提高可恢复性；复杂集成放到最新 main 派生的非生产分支；部署最后做。</p>",
  "HINT_3_HTML": "<p>最终证明不是“SHA 看起来一样”，而是 <code>GitHub main = origin/main = local main = M</code>，并且运行镜像/二进制能够追溯到 M，health/smoke 通过。</p>",
  "FINAL_ANSWER_HTML": "\n<p><strong>参考答案的主线：把“同步 Git”从一个命令问题改写成一个状态收敛问题。</strong></p>\n\n<h3>阶段 0：定义权威与禁止动作</h3>\n<p>先定义 GitHub <code>main</code> 是最终 canonical 线，但在完成独有历史清点之前，禁止任何会降低可恢复性的操作：<code>reset --hard</code>、<code>clean -fd</code>、删除分支/worktree、强推覆盖远端。canonical 并不意味着其他状态可以不经检查直接丢弃。</p>\n\n<h3>阶段 1：只读 inventory</h3>\n<pre><code>git status --porcelain=v1\ngit rev-parse HEAD\ngit rev-parse main\ngit rev-parse origin/main\ngit worktree list --porcelain\ngit stash list\ngit branch --all --verbose --no-abbrev\n\ngit merge-base HEAD origin/main\ngit rev-list --left-right --count HEAD...origin/main\ngit log --oneline origin/main..HEAD</code></pre>\n<p>输出要结构化记录：每个 ref 的 SHA、比较对象、ahead/behind、独有 commit、dirty files、stash、worktree。此阶段目标不是修，而是把“未知”变成事实。</p>\n\n<h3>阶段 2：保护所有独有信息</h3>\n<pre><code>git branch backup/server-c5e35ae c5e35ae\ngit branch backup/frontend-a0580dec a0580dec\n\n# 如果需要跨机器恢复，应把明确 backup ref 推到受控远端\n# git push origin backup/server-c5e35ae\n# git push origin backup/frontend-a0580dec</code></pre>\n<p>若 worktree 有未提交内容，先明确 commit 到备份分支或用可审计方式保存；stash 只能当临时手段，不应让关键状态长期只存在 stash 中。</p>\n\n<h3>阶段 3：以最新 main 创建非生产集成线</h3>\n<pre><code>git fetch origin\ngit switch -c integrate/frontend-20260831 origin/main\n\n# 服务器独有热修仍有价值时，先显式重放\ngit cherry-pick c5e35ae\n\n# 再集成前端历史（示意）\ngit merge backup/frontend-a0580dec</code></pre>\n<p>如果出现 48 个冲突，逐功能处理，并结合最新 main 的架构判断保留语义。不能把“全部 ours”或“全部 theirs”当默认策略。冲突解决后跑单元测试、构建、关键浏览器/CLI smoke 和必要回归。</p>\n\n<h3>阶段 4：只有验收通过才更新 main</h3>\n<p>假设验收后的 commit 为 M。此时才把 canonical <code>main</code> fast-forward/合并到 M 并推送。CI 与评审关注的是 M 的完整证据，而不是“我的集成分支本地能跑”。</p>\n\n<h3>阶段 5：服务器部署收敛</h3>\n<pre><code>git fetch origin\ngit switch main\ngit merge --ff-only origin/main\n\ngit rev-parse HEAD\ngit rev-parse origin/main\ngit status --porcelain=v1</code></pre>\n<p><code>--ff-only</code> 的价值是：如果服务器 main 有未解释分叉，它会直接拒绝偷偷制造新的 merge commit。若失败，重新回到 inventory，而不是立刻 hard reset。</p>\n\n<h3>阶段 6：证明真正运行的是 M</h3>\n<p>这是很多 Git 教程缺的一步。若系统通过 Docker、Go 二进制、Node build 或其他 artifact 发布，还要核对：</p>\n<pre><code># 例：应用自己暴露版本\ncurl -sS https://service.example.com/version\n\n# 例：Docker\ndocker inspect --format '{{.Image}}' app\ndocker image inspect IMAGE --format '{{json .RepoDigests}}'\n\n# 再做业务 smoke / health\ncurl -fsS https://service.example.com/health</code></pre>\n<p>最佳实践是在 build 时把 commit SHA 写入版本端点、二进制 metadata 或 OCI image labels。这样能验证 <code>runtime → artifact → source commit M</code> 的可追溯关系。</p>\n\n<h3>八条安全不变量</h3>\n<ol>\n<li>任何破坏性命令之前，所有独有 commit 和未提交状态都必须有明确可恢复引用或归档。</li>\n<li>所有 ahead/behind 结论必须携带“比较的是哪两个 refs”。</li>\n<li><code>origin/main</code> 只代表 fetch 后的远端跟踪状态，不冒充 HEAD。</li>\n<li>clean 只证明未提交状态，不冒充 up-to-date。</li>\n<li>生产服务器不作为复杂冲突的首选集成场所。</li>\n<li>公共共享历史不在没有协调的情况下被 rebase/force-push 改写。</li>\n<li>canonical main 更新前必须有针对集成结果的测试证据。</li>\n<li>Git SHA 收敛后必须继续验证真正运行的 artifact 与同一 SHA 对应。</li>\n</ol>\n\n<div class=\"call success\"><strong>最终完成标准：</strong>① 需要保留的前端/服务器独有语义已进入 canonical main 或被明确归档；② local main、origin/main、GitHub main 对应同一已验收 commit M；③ 所有工作区状态可解释；④ 生产 artifact 可追溯到 M；⑤ health/smoke 通过。到这一步才叫“收敛完成”。</div>\n\n<h3>迁移能力</h3>\n<p>这套模型不只用于你这次的 106 服务器。以后遇到 Agent 并行分支、多个 worktree、CI checkout、release branch、hotfix、回滚，只需要持续问四个问题：<strong>对象图里发生了什么？哪些 refs 指向哪里？index/working tree 有没有独有状态？真正运行的 artifact 来自哪个 commit？</strong> 只要这四层分开，Git 就不再是一组玄学命令。</p>\n<p>进一步阅读：<a href=\"https://git-scm.com/docs/gitrevisions\">gitrevisions</a> · <a href=\"https://git-scm.com/docs/git-merge-base\">git merge-base</a> · <a href=\"https://git-scm.com/docs/git-reset\">git reset</a> · <a href=\"https://git-scm.com/docs/git-rebase\">git rebase</a>。</p>\n"
}
